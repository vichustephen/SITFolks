<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seven.zion.sitfolks">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".SITFolks"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RegisterActivity"
            android:label="Register"
            android:theme="@style/Main" />
        <activity
            android:name=".Results"
            android:label="@string/result"
            android:theme="@style/Main">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Main" />
        <activity
            android:name=".Canteen"
            android:label="Canteen"
            android:theme="@style/Main" />
        <activity android:name=".QRcode" />
        <activity
            android:name=".AdminActivity"
            android:label="QRCode Scanner"
            android:theme="@style/Main" />
        <activity android:name=".OrderFullFill" />
        <activity android:name=".post" />
        <activity
            android:name=".editProfile"
            android:label="Edit Profile"
            android:theme="@style/Main" />
        <activity
            android:name=".CGPA_calculator"
            android:label="CGPA Calculator"
            android:theme="@style/Main" />
        <activity
            android:name=".About_us"
            android:label="About Us"
            android:theme="@style/Main" />
        <activity
            android:name=".ImageviewScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_imageview_screen"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".portal"></activity>
    </application>

</manifest>